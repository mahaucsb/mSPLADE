git clone git@github.com:mahaucsb/mSPLADE.git
cd into the folder
git init
git add *
git commit -m 'first commit'
git branch -a  //to see what's the head name, master or main
git push -u origin <master> or <main>
git remote -v 
git status
